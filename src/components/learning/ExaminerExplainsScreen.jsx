// Legacy compatibility export.
// New code should import WhatExaminersLookFor directly. The old filename remains
// temporarily so existing module routes and content do not break during migration.
export { default } from './WhatExaminersLookFor.jsx'
