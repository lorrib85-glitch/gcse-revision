import App from './App'

export default {
  component: App,
  tags: ['ai-generated'],
  parameters: {
    layout: 'fullscreen',
    viewport: { defaultViewport: 'mobile1' },
  },
}

// Current home screen — starting point for the redesign
export const HomeScreen = {
  name: 'Home (current)',
}
