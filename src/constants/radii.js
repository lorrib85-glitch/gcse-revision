// ─── Radius System — Locked Foundation Layer ─────────────────────────────────
//
// ALL corner radii across the product use these tokens.
// Prevents radius drift, keeps UI grounded and premium.
//
// Do NOT use pill radius for primary CTAs.
// Primary buttons must not become jellybean-shaped.

export const RADII = {
  small: 10,
  medium: 16,
  large: 22,
  panel: 26,
  pill: 999,
}
